---
import Layout from '../layouts/Layout.astro';

---

<Layout title="Welcome to Astro.">
	<span class="font-MyFont">Hola mundo</span>
	<div class="text-red-500">Hola</div>
	<div id="btn" class="bg-blue-200 p-3 inline-block">
		Prersióname!
	</div>
	
	<div class="embla">
		<div class="embla__container">
		  <div class="embla__slide bg-blue-400">Slide 1</div>
		  <div class="embla__slide bg-green-500">Slide 2</div>
		  <div class="embla__slide bg-red-500">Slide 3</div>
		</div>
		<button class="embla__prev">Prev</button>
  		<button class="embla__next">Next</button>
	  </div>
	  
</Layout>
<style>
	.embla {
  overflow: hidden;
}
.embla__container {
  display: flex;
}
.embla__slide {
  flex: 0 0 100%;
  min-width: 0;
}
</style>
<script>
	import JSConfetti from 'js-confetti';
	import EmblaCarousel from 'embla-carousel';


	const btn = document.getElementById("btn")
	const jsConfetti = new JSConfetti()

	const rootNode = document.querySelector('.embla')
	const viewportNode = rootNode.querySelector('.embla__viewport')
	const prevButtonNode = rootNode.querySelector('.embla__prev')
	const nextButtonNode = rootNode.querySelector('.embla__next')
	const options = { loop: false }
	const embla = EmblaCarousel(viewportNode, options)
	prevButtonNode.addEventListener('click', embla.scrollPrev, false)
	nextButtonNode.addEventListener('click', embla.scrollNext, false)

	btn.addEventListener("click",function(e) {
		
		const elm = e.target
		elm.classList.replace("bg-blue-200","bg-red-500")
		elm.innerText = "Ouch!"

		const myDiv = document.createElement("div")
		myDiv.innerText = "Soy un div creado con 35"
		myDiv.classList.add("bg-green-500", "text-white")

		myDiv.addEventListener("click", (e) => {
			myDiv.remove()
			console.log("Harakiri", e.target)
		})

		document.body.appendChild(myDiv)
		//console.log("Acá en la consola", e.target)

		jsConfetti.addConfetti(
		{
  			emojis: ['🦁'],
  			emojiSize: 100,
  			confettiNumber: 30,
			}
		)
	})


	
</script>